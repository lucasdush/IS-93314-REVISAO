# PROD
# base html
    git push ( mandar )
    git checkout STAGING ( mudar branch )
    git add .
    git commit -m "Edicao   "
    git pull ( finalizar )
    git add .
    history
    gh auth logout
    gh auth login
    

git checkout -b feat-base-html (criar um branch)
git merge feat-base-html (mover para brach )
git branch -d  ( DELETAR branch )
git checkout -b fix-css ( branch de correção )
